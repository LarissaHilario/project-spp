import { createTheme } from '@mui/material';

const Theme = createTheme({
  palette: {
    primary: {
      main: '#5C3DA4',
    },
    secondary: {
        main: '#B89CF9',
      },
    third: {
        main: '#FE92A1',
      },
  },
});

export default Theme;
